# PRの書き方ガイド（このプロジェクト用）

このガイドは、`input-handler / ai-handler / save-handler` の3Lambda構成を前提に、**レビューしやすく、事故りにくい**PRの書き方をまとめたものです。  
（そのままコピペして使えるテンプレ付き）

---

## 1. PRタイトルの付け方（おすすめ）

- `feat:` 機能追加
- `fix:` バグ修正
- `chore:` 雑務（依存更新・設定変更・整理）
- `docs:` ドキュメント
- `refactor:` 仕様変更なしの整理

**例**
- `feat: add input payload validation`
- `fix: correct danger score threshold`
- `chore: update ai-handler dependencies`

> タイトルだけで「何のPRか」が分かるのが理想です。

---

## 2. PR本文に必ず書くべきこと（最小セット）

PR本文は、以下の3点が揃えばOKです。

1. **何をしたか（概要）**
2. **なぜやったか（背景/目的）**
3. **どう確認したか（動作確認）**

さらにこのプロジェクトでは、別ファイルでも壊れることがあるため、次も重要です。

- **影響範囲**（どのLambda/どの層に影響するか）
- **イベントスキーマ変更の有無**（input→ai / ai→save のキーや型の変更があるか）

---

## 3. 影響範囲（このプロジェクトの注意点）

「同じファイルを触っていない」でも壊れる典型例：

- Aが `input-handler` の出力イベントのキー名/構造を変更  
  → Bの `ai-handler` が古いキー前提で落ちる
- Bが `danger_score` の戻り値（型・意味）を変更  
  → Cの `save-handler` の保存形式とズレる

**ルール（PRで必ず明記）**
- イベントスキーマ変更があるなら `スキーマ変更：あり` と書く
- 破壊的変更は可能なら段階的に（新キー追加→両対応→旧キー削除）

---

## 4. PRテンプレ（コピペで使う用）

以下をPR本文に貼って埋めてください。

```md
## 概要（1〜2行）
何をしたPRかを短く書く

## 背景 / 目的
なぜ必要か（バグ、要件、改善点）

## 変更内容
- [ ] 変更点1
- [ ] 変更点2

## 影響範囲
- 対象：input-handler / ai-handler / save-handler / infrastructure / shared
- 破壊的変更：あり / なし
- イベントスキーマ（input→ai / ai→save）変更：あり / なし

## 動作確認（必須）
- [ ] ローカルで実行した（可能な範囲で）
- [ ] 主要ケースでエラーにならないことを確認
### 確認手順
1.
2.
3.

## スクリーンショット / ログ（任意）
（CloudWatchログの抜粋など）

## ロールバック手順（任意）
（戻すときにどうするか。影響が大きい場合だけ）

## チェックリスト
- [ ] Secrets（APIキー等）をコミットしていない
- [ ] requirements.txt を更新した（必要な場合）
- [ ] README / 契約（スキーマ）ドキュメントを更新した（必要な場合）
- [ ] CIが通っている
```

---

## 5. 良いPRのコツ（短く）

- **リファクタと仕様変更を混ぜない**（レビューが爆速になる）
- **PRを小さく**（「説明が1文で済む」粒度が目安）
- 依存追加したら **requirements.txt も同じPRで更新**
- Secrets（トークン/鍵/URL等）を入れない  
  → 共有したい場合は `.env.example` で変数名だけ共有する

---

## 6. 置き場所（おすすめ）

- PRテンプレとして自動で出したい場合：  
  `.github/pull_request_template.md` に保存（GitHubが自動で挿入）

- このガイド自体は：  
  `docs/PR_GUIDE.md` などに置くとチーム共有しやすいです
